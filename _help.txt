Mostly everything in Dart is a class, and objects are instances of these classes (numbers, functions and even null are objects).

Collection-if/for/spread(...list, ...map): if (addblue) 'blue',
158. video: Dartx

dart create
code .
flutter pub get
dart main.dart

dart pub outdated --mode=null-safety
dart pub upgrade --null-safety
dart pub get
dart migrate
dart analyze     # or `flutter analyze`
dart test     # or `flutter analyze`

TYPE CONVERSION:
double.parse()
int.tryparse('123')
.toInt() .toDouble()
round()
toStringAsFixed(1)

//TODO
/// documentation

INPUT:
import 'dart:io';
stdin.readLineSync();
stdout.write('');

RANDOM:
import 'math:io';
rng = Random()
rng.nextInt(3)

STRING:
.toUpper()
.contains()
line.split(',');
myString.replaceAll('"','');

LIST (COLLECTIONS):
list.length
list.isEmpty
list.isNotEmpty
list.first
list.last
list.add('new item');
list.addAll(otherList);
list.insert(1, 'new item');
list.remove('item');
list.removeAt(0);
list.clear();
list.contains('item');
cities.indexOf('item');
list2 = [...list1];

list.forEach(print);
list.map((value) => value * 2); (map creates a lazy iterable, so toList() can help) Iterable<T> map<T>(T Function(int) f)
list.where((value) => value % 2 ==0);
list.firstWhere((x) => x == 5, orElse: () => -1);

final sum = list.reduce((value, element) => value + element);


SET:
thisIsASet = {'Italy', 'UK', 'Russia'};
thisIsASet.elementAt(0);
thisIsASet.union(anotherSet)
thisIsASet.intersection(anotherSet)
thisIsASet.difference(anotherSet)

MAPS:
if (mymap['something']) == null)
mymap.keys/values/entries -> entry.key, entry.value
mymap.containsKey(mykey)
mymap.isEmpty

for(var city in cities) {
	print(city?.toUpperCase());
} // if not null execute toUpperCase(), otherwise print null)

FILE:
File(inputFile).readAsLinesSync();

NULL SAFETY (everything non-nullable must be initialized right away (classes), or through flow analysis (functions), or late)
int? value;
int sign = x >= 0 ? 1 : -1;
int newValue = myValue! (assertion)
int newValue = myValue ?? 0; (if null)  //? if not null
newValue ??= 0; (augmented assignment if-null operator: assign 0 only if it is currently null)

for (var city in cities) {
 print(city?.toUpperCase());
} // null city values will also be displayed without error

EQUATABLE (only use with immutable classes => properties must be final)
dependencies:
  equatable: ^2.0.3
  
class Point extends Equatable {
  const Point(this.x, this.y);
  final int x;
  final int y;

  @override
  List<Object?> get props => [x,y];
  
CASCADE(Path? path;)
?..
(not needed for non-nullable: Path path = Path();)
..

----
void main() {
  final list2 = (([1, 0, 2]..sort()).reversed.toList()
    ..addAll([5, 3, 4])
    ..sort())
    .map((e) => e + 1).toList();
  print(list2);
}
----